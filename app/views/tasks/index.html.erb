<h1>Task List</h1>

<% @tasks.each do |task| %>
  <p><%= link_to task.name, task_path(task) %></p>
  <p><%= link_to 'Edit task', edit_task_path(task) %></p>
  <p><%= link_to 'Delete task', task_path(task), method: :delete %></p>
<% end %>

<%= link_to 'Create a new task', new_task_path %>
